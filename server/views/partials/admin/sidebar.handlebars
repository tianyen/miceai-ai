<nav class="sidebar">
    <div class="sidebar-header">
        <div class="logo">🎯</div>
        <h3>MICE AI 管理系統</h3>
        <p style="font-size: 12px; opacity: 0.7; margin-top: 5px;">
            <span data-user="name">{{#if user.full_name}}{{user.full_name}}{{else}}{{#if
                user.username}}{{user.username}}{{else}}載入中...{{/if}}{{/if}}</span>
            (<span data-user="role">{{#if user.role}}{{user.role}}{{else}}載入中...{{/if}}</span>)
        </p>
        <button class="sidebar-toggle" onclick="toggleSidebar()" title="收合側邊欄">
            <i class="fas fa-chevron-left"></i>
        </button>
    </div>

    <div class="sidebar-nav">
        <div class="nav-section">主要功能</div>

        <div class="nav-item">
            <a href="/admin/dashboard" class="nav-link{{#if (eq currentPage 'dashboard')}} active{{/if}}">
                <i class="fas fa-tachometer-alt"></i>
                儀表板
            </a>
        </div>

        <!-- 活動管理 -->
        <div class="nav-section">活動管理</div>

        <div class="nav-item">
            <a href="/admin/projects" class="nav-link{{#if (eq currentPage 'projects')}} active{{/if}}">
                <i class="fas fa-calendar-alt"></i>
                活動專案
            </a>
        </div>

        <div class="nav-item nav-sub-item">
            <a href="/admin/checkin-management"
                class="nav-link{{#if (eq currentPage 'checkin-management')}} active{{/if}}">
                <i class="fas fa-check-circle"></i>
                報到管理
            </a>
        </div>

        <div class="nav-item">
            <a href="/admin/templates" class="nav-link{{#if (eq currentPage 'templates')}} active{{/if}}">
                <i class="fas fa-calendar-check"></i>
                活動模板
            </a>
        </div>

        <div class="nav-item">
            <a href="/admin/submissions" class="nav-link{{#if (eq currentPage 'submissions')}} active{{/if}}">
                <i class="fas fa-clipboard-list"></i>
                表單數據
            </a>
        </div>

        <div class="nav-item">
            <a href="/admin/trace-tracking" class="nav-link{{#if (eq currentPage 'trace-tracking')}} active{{/if}}">
                <i class="fas fa-route"></i>
                參加者追蹤
            </a>
        </div>

        <div class="nav-item">
            <a href="/admin/user-tracking" class="nav-link{{#if (eq currentPage 'user-tracking')}} active{{/if}}">
                <i class="fas fa-user-clock"></i>
                用戶追蹤
            </a>
        </div>

        <!-- 問卷管理 -->
        <div class="nav-section">問卷</div>

        <div class="nav-item">
            <a href="/admin/questionnaire/stats"
                class="nav-link{{#if (eq currentPage 'questionnaire-stats')}} active{{/if}}">
                <i class="fas fa-chart-bar"></i>
                問卷統計
            </a>
        </div>

        <div class="nav-item nav-sub-item">
            <a href="/admin/questionnaire/design"
                class="nav-link{{#if (eq currentPage 'questionnaire-design')}} active{{/if}}">
                <i class="fas fa-edit"></i>
                問卷設計
            </a>
        </div>

        <div class="nav-item nav-sub-item">
            <a href="/admin/questionnaire/qr" class="nav-link{{#if (eq currentPage 'questionnaire-qr')}} active{{/if}}">
                <i class="fas fa-qrcode"></i>
                問卷 QR Code
            </a>
        </div>

        <!-- 遊戲室 -->
        <div class="nav-section">遊戲室</div>

        <div class="nav-item">
            <a href="/admin/games" class="nav-link{{#if (eq currentPage 'games')}} active{{/if}}">
                <i class="fas fa-gamepad"></i>
                遊戲管理
            </a>
        </div>

        <div class="nav-item">
            <a href="/admin/vouchers" class="nav-link{{#if (eq currentPage 'vouchers')}} active{{/if}}">
                <i class="fas fa-ticket-alt"></i>
                兌換券管理
            </a>
        </div>

        <div class="nav-item nav-sub-item">
            <a href="/admin/vouchers/scanner" class="nav-link{{#if (eq currentPage 'voucher-scanner')}} active{{/if}}">
                <i class="fas fa-qrcode"></i>
                兌換券掃描
            </a>
        </div>

        <div class="nav-item">
            <a href="/admin/booths" class="nav-link{{#if (eq currentPage 'booths')}} active{{/if}}">
                <i class="fas fa-store"></i>
                攤位管理
            </a>
        </div>

        <!-- 系統管理功能 - 僅超級管理員可見 -->
        {{!-- {{#if user}}
        {{#if user.role}}
        {{#eq user.role 'super_admin'}} --}}
        <div class="nav-section admin-only">系統管理</div>

        <div class="nav-item admin-only">
            <a href="/admin/users" class="nav-link{{#if (eq currentPage 'users')}} active{{/if}}">
                <i class="fas fa-users"></i>
                用戶管理
            </a>
        </div>

        <div class="nav-item admin-only">
            <a href="/admin/logs" class="nav-link{{#if (eq currentPage 'logs')}} active{{/if}}">
                <i class="fas fa-history"></i>
                系統日誌
            </a>
        </div>
        {{!-- {{/eq}}
        {{/if}}
        {{/if}} --}}

        <!-- 專案管理員功能 -->
        {{!-- {{#if user}}
        {{#ifCond user.role '==' 'project_manager'}} --}}
        <div class="nav-section">專案管理</div>

        <div class="nav-item">
            <a href="/admin/users" class="nav-link{{#if (eq currentPage 'users')}} active{{/if}}">
                <i class="fas fa-user-friends"></i>
                團隊管理
            </a>
        </div>
        {{!-- {{/ifCond}}
        {{/if}} --}}

        <div class="nav-section">其他</div>

        <div class="nav-item">
            <a href="/admin/profile" class="nav-link{{#if (eq currentPage 'profile')}} active{{/if}}">
                <i class="fas fa-user-circle"></i>
                個人資料
            </a>
        </div>

        <div class="nav-item">
            <a href="/" target="_blank" class="nav-link">
                <i class="fas fa-external-link-alt"></i>
                前台預覽
            </a>
        </div>

        <div class="nav-item">
            <a href="/admin/logout" class="nav-link">
                <i class="fas fa-sign-out-alt"></i>
                登出
            </a>
        </div>
    </div>
</nav>