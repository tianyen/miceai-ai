<nav class="sidebar">
    <div class="sidebar-header">
        <div class="logo">🎯</div>
        <h3>MICE AI 管理系統</h3>
        <p style="font-size: 12px; opacity: 0.7; margin-top: 5px;">
            <span data-user="name">{{#if user.full_name}}{{user.full_name}}{{else}}{{#if
                user.username}}{{user.username}}{{else}}載入中...{{/if}}{{/if}}</span>
            {{#if isCheckinOperator}}
            <span style="color: #f39c12;">(報到專員)</span>
            {{else}}
            (<span data-user="role">{{#if user.role}}{{user.role}}{{else}}載入中...{{/if}}</span>)
            {{/if}}
        </p>
        <button class="sidebar-toggle" onclick="toggleSidebar()" title="收合側邊欄">
            <i class="fas fa-chevron-left"></i>
        </button>
    </div>

    <div class="sidebar-nav">
        {{#if isCheckinOperator}}
        {{!-- 報到專員簡化選單 --}}
        <div class="nav-section">報到功能</div>

        {{#each allowedProjects}}
        <div class="nav-item">
            <a href="/admin/projects/{{this}}/detail" class="nav-link" title="專案報到管理">
                <i class="fas fa-clipboard-check"></i>
                <span>手動報到</span>
            </a>
        </div>

        <div class="nav-item">
            <a href="/admin/qr-scanner?project_id={{this}}" class="nav-link" title="QR Code 掃描">
                <i class="fas fa-qrcode"></i>
                <span>QR 掃描報到</span>
            </a>
        </div>
        {{/each}}

        <div class="nav-section">其他</div>

        <div class="nav-item">
            <a href="#" class="nav-link" title="登出" onclick="logoutAdmin(); return false;">
                <i class="fas fa-sign-out-alt"></i>
                <span>登出</span>
            </a>
        </div>

        {{else}}
        {{!-- 一般用戶完整選單 --}}
        <div class="nav-section">主要功能</div>

        <div class="nav-item">
            <a href="/admin/dashboard" class="nav-link{{#if (eq currentPage 'dashboard')}} active{{/if}}" title="儀表板">
                <i class="fas fa-tachometer-alt"></i>
                <span>儀表板</span>
            </a>
        </div>

        <!-- 活動管理 -->
        <div class="nav-section">活動管理</div>

        <div class="nav-item">
            <a href="/admin/projects" class="nav-link{{#if (eq currentPage 'projects')}} active{{/if}}" title="活動專案">
                <i class="fas fa-calendar-alt"></i>
                <span>活動專案</span>
            </a>
        </div>

        <div class="nav-item nav-sub-item">
            <a href="/admin/booths" class="nav-link{{#if (eq currentPage 'booths')}} active{{/if}}" title="攤位管理">
                <i class="fas fa-store"></i>
                <span>攤位管理</span>
            </a>
        </div>

        <div class="nav-item nav-sub-item">
            <a href="/admin/checkin-management"
                class="nav-link{{#if (eq currentPage 'checkin-management')}} active{{/if}}" title="報到管理">
                <i class="fas fa-check-circle"></i>
                <span>報到管理</span>
            </a>
        </div>

        <div class="nav-item">
            <a href="/admin/templates" class="nav-link{{#if (eq currentPage 'templates')}} active{{/if}}" title="活動模板">
                <i class="fas fa-calendar-check"></i>
                <span>活動模板</span>
            </a>
        </div>

        <div class="nav-item">
            <a href="/admin/submissions" class="nav-link{{#if (eq currentPage 'submissions')}} active{{/if}}" title="表單數據">
                <i class="fas fa-clipboard-list"></i>
                <span>表單數據</span>
            </a>
        </div>

        <div class="nav-item">
            <a href="/admin/trace-tracking" class="nav-link{{#if (eq currentPage 'trace-tracking')}} active{{/if}}" title="參加者追蹤">
                <i class="fas fa-route"></i>
                <span>參加者追蹤</span>
            </a>
        </div>

        <div class="nav-item">
            <a href="/admin/email-management" class="nav-link{{#if (eq currentPage 'email-management')}} active{{/if}}" title="邀請信管理">
                <i class="fas fa-envelope"></i>
                <span>邀請信管理</span>
            </a>
        </div>

        <!-- 問卷管理 -->
        <div class="nav-section">問卷</div>

        <div class="nav-item">
            <a href="/admin/questionnaire/stats"
                class="nav-link{{#if (eq currentPage 'questionnaire-stats')}} active{{/if}}" title="問卷統計">
                <i class="fas fa-chart-bar"></i>
                <span>問卷統計</span>
            </a>
        </div>

        <div class="nav-item nav-sub-item">
            <a href="/admin/questionnaire/design"
                class="nav-link{{#if (eq currentPage 'questionnaire-design')}} active{{/if}}" title="問卷設計">
                <i class="fas fa-edit"></i>
                <span>問卷設計</span>
            </a>
        </div>

        <div class="nav-item nav-sub-item">
            <a href="/admin/questionnaire/qr" class="nav-link{{#if (eq currentPage 'questionnaire-qr')}} active{{/if}}" title="問卷 QR Code">
                <i class="fas fa-qrcode"></i>
                <span>問卷 QR Code</span>
            </a>
        </div>

        <!-- 遊戲室 -->
        <div class="nav-section">遊戲室</div>

        <div class="nav-item">
            <a href="/admin/games" class="nav-link{{#if (eq currentPage 'games')}} active{{/if}}" title="遊戲">
                <i class="fas fa-gamepad"></i>
                <span>遊戲</span>
            </a>
        </div>

        <div class="nav-item nav-sub-item">
            <a href="/admin/game-analytics" class="nav-link{{#if (eq currentPage 'game-analytics')}} active{{/if}}" title="遊戲分析">
                <i class="fas fa-chart-line"></i>
                <span>遊戲分析</span>
            </a>
        </div>

        <div class="nav-item">
            <a href="/admin/vouchers" class="nav-link{{#if (eq currentPage 'vouchers')}} active{{/if}}" title="兌換券">
                <i class="fas fa-ticket-alt"></i>
                <span>兌換券</span>
            </a>
        </div>

        <div class="nav-item nav-sub-item">
            <a href="/admin/vouchers/stats" class="nav-link{{#if (eq currentPage 'voucher-stats')}} active{{/if}}" title="兌換券統計">
                <i class="fas fa-chart-bar"></i>
                <span>兌換券統計</span>
            </a>
        </div>

        <div class="nav-item nav-sub-item">
            <a href="/admin/vouchers/scanner" class="nav-link{{#if (eq currentPage 'voucher-scanner')}} active{{/if}}" title="兌換券掃描">
                <i class="fas fa-qrcode"></i>
                <span>兌換券掃描</span>
            </a>
        </div>

        <!-- 系統管理功能 - 僅超級管理員可見 -->
        {{!-- {{#if user}}
        {{#if user.role}}
        {{#eq user.role 'super_admin'}} --}}
        <div class="nav-section admin-only">系統管理</div>

        <div class="nav-item admin-only">
            <a href="/admin/users" class="nav-link{{#if (eq currentPage 'users')}} active{{/if}}" title="用戶管理">
                <i class="fas fa-users"></i>
                <span>用戶管理</span>
            </a>
        </div>

        <div class="nav-item admin-only">
            <a href="/admin/logs" class="nav-link{{#if (eq currentPage 'logs')}} active{{/if}}" title="系統日誌">
                <i class="fas fa-history"></i>
                <span>系統日誌</span>
            </a>
        </div>
        {{!-- {{/eq}}
        {{/if}}
        {{/if}} --}}

        <div class="nav-section">其他</div>

        <div class="nav-item">
            <a href="/admin/profile" class="nav-link{{#if (eq currentPage 'profile')}} active{{/if}}" title="個人資料">
                <i class="fas fa-user-circle"></i>
                <span>個人資料</span>
            </a>
        </div>

        <div class="nav-item">
            <a href="/" target="_blank" class="nav-link" title="前台預覽">
                <i class="fas fa-external-link-alt"></i>
                <span>前台預覽</span>
            </a>
        </div>

        <div class="nav-item">
            <a href="#" class="nav-link" title="登出" onclick="logoutAdmin(); return false;">
                <i class="fas fa-sign-out-alt"></i>
                <span>登出</span>
            </a>
        </div>
        {{/if}}
    </div>
</nav>